#include <iostream>
using namespace std;


string replace_str(string input, string pattern, string to){

    for(int i = 0; i < input.size(); ++i){
        if(input[i] == pattern[0] && input[i] == pattern[0]);
    }
}

int main() {


    return 0;
}